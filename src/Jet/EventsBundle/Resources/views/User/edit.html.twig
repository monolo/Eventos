{% extends 'JetEventsBundle::layout.html.twig' %}
{% block title %}Editando User{% endblock %}
{% form_theme form "JetEventsBundle:Form:fields.html.twig" %}
{% block main %}
<form action="{{ path('profile_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-success" type="submit">Edit</button>
    </p>
</form>

<a class="btn btn-info" href="{{ path('profile') }}">
            Volver atrás
        </a>
        <form action="{{ path('profile_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button class="btn btn-danger" type="submit">Borrar</button>
        </form>

{% endblock %}

